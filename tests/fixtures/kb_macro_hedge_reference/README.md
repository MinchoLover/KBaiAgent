# kb_macro hedge adapter test fixture

이 디렉터리의 두 JSON은 KBaiAgent 외부 헤지 어댑터의 API-free 회귀
검증용으로 축소한 합성 fixture다.

- 공식 `kb_macro_ai` request/response JSON Schema가 아니다.
- 실제 가격, 고객 거래 또는 모델 원문 응답이 아니다.
- 외부 모델 정확도나 실제 상품 추천을 주장하는 자료로 사용할 수 없다.
- 원격 producer commit과 실제 실행 파일 SHA는 실행 설정에서 별도로 고정한다.
